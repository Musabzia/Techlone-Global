import React from 'react';
import IllustrationComponent from '../components/IllustrationComponent'; // Renaming import to avoid conflict

const Illustrations = () => {
    return (
        <div>
          <IllustrationComponent /> 
       
        </div>
    );
};

export default Illustrations;